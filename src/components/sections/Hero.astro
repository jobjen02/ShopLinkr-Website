---
import HeroOrder from '../product/HeroOrder.vue';
import { externalLinks } from '../../data/externalLinks';
import { type Locale } from '../../i18n/routes';
import { useTranslations } from '../../i18n/ui';

const locale = (Astro.currentLocale ?? 'nl') as Locale;
const t = useTranslations(locale);
---

<section class="relative pt-12 pb-20 md:pt-16 md:pb-28 overflow-hidden">
    <div class="absolute inset-0 -z-10 pointer-events-none">
        <div class="absolute -top-24 right-[-8%] w-[62%] h-[95%] bg-[radial-gradient(ellipse_at_center,#faedd5,transparent_70%)] opacity-70 blur-3xl"></div>
    </div>

    <div class="container-prose">
        <div class="grid lg:grid-cols-[1.05fr_1fr] gap-12 lg:gap-16 items-center">
            <div>
                <p class="inline-flex items-center gap-2 text-xs font-medium text-gravel mb-6">
                    <span class="h-1.5 w-1.5 rounded-full bg-sunstone" aria-hidden="true"></span>
                    {t.hero.badge}
                </p>

                <h1 class="text-[2.25rem] sm:text-[2.75rem] md:text-[3.5rem] font-semibold text-charcoal leading-[1.05] tracking-tight">
                    {t.hero.heading}
                </h1>

                <p class="mt-6 text-lg md:text-xl text-steel leading-relaxed max-w-xl">
                    {t.hero.sub}
                </p>

                <div class="mt-9 flex flex-col sm:flex-row gap-3">
                    <a
                        href={externalLinks.register}
                        class="btn btn-sunstone text-base"
                        target="_blank"
                        rel="noopener noreferrer"
                    >
                        {t.common.register}
                        <i class="fa-solid fa-arrow-right text-sm" aria-hidden="true"></i>
                    </a>
                    <a
                        href={externalLinks.demoBooking}
                        class="btn btn-secondary text-base"
                        target="_blank"
                        rel="noopener noreferrer"
                    >
                        {t.common.demo}
                    </a>
                </div>

                <p class="mt-5 text-sm text-gravel">
                    {t.hero.fineprint}
                </p>
            </div>

            <div class="relative mt-4 lg:mt-0">
                <HeroOrder client:visible locale={locale} />
            </div>
        </div>
    </div>
</section>
