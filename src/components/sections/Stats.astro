---
// Stats section intentionally left empty.
//
// Rationale: shoplinkr.com (live) uses qualitative social proof (testimonials + Trustpilot badge)
// and does NOT have a dedicated stats strip with metric cards. Three previous design iterations
// (charcoal block, paper border-y, sunstone-mist icon cards) were rejected by the owner.
//
// Numbers live elsewhere on the page where they fit naturally:
// - "4,9 op Trustpilot"     -> already in Testimonials header
// - "110.000+ orders"       -> should move to Hero subline or TrustBar eyebrow (see report)
// - "< 24u support"         -> should move to CtaBanner trust-bullets (next to "14 dagen gratis")
// - "€57 / mnd"             -> should move to CtaBanner trust-bullets or near Pricing teaser
//
// This file is kept as a no-op so the import in pages/index.astro keeps working.
// Remove the <Stats /> import and tag from index.astro to delete it entirely.
---
