---
import PageLayout from '../../layouts/PageLayout.astro';
import CtaBanner from '../sections/CtaBanner.astro';
import FaqList from '../template/FaqList.astro';
import { externalLinks } from '../../data/externalLinks';
import { breadcrumbs } from '../../utils/seo';
import { faqsByPage } from '../../data/faqs';
import { faqsByPageEn } from '../../data/faqs-en';
import { type Locale, localizeHref } from '../../i18n/routes';
import { useTranslations } from '../../i18n/ui';

const locale = (Astro.currentLocale ?? 'nl') as Locale;
const t = useTranslations(locale);
const p = t.pages.featureUsers;

// Icons are locale-invariant; merge them with the per-locale card copy by index.
const subFeatureIcons = [
    'fa-duotone fa-solid fa-user-shield',
    'fa-duotone fa-solid fa-envelope-open-text',
    'fa-duotone fa-solid fa-qrcode',
    'fa-duotone fa-solid fa-shield-halved',
];
const subFeatures = p.subFeatures.map((feature, i) => ({ ...feature, icon: subFeatureIcons[i] }));

const permissionTypes = p.permissionTypes;

const faqs = locale === 'en' ? faqsByPageEn.gebruikers : faqsByPage.gebruikers;

const jsonLd = [
    breadcrumbs([
        ['Home', localizeHref('/', locale)],
        [p.breadcrumbParent, localizeHref('/functionaliteiten', locale)],
        [p.breadcrumbLeaf, localizeHref('/functionaliteiten/gebruikers', locale)],
    ]),
    {
        '@type': 'WebPage',
        name: p.jsonLdName,
        description: p.jsonLdDescription,
    },
    {
        '@type': 'FAQPage',
        mainEntity: faqs.map((faq) => ({
            '@type': 'Question',
            name: faq.question,
            acceptedAnswer: {
                '@type': 'Answer',
                text: faq.answer,
            },
        })),
    },
];
---

<PageLayout
    title={p.title}
    description={p.description}
    eyebrow={p.eyebrow}
    heading={p.heading}
    subheading={p.subheading}
    routeKey="features.users"
    jsonLd={jsonLd}
>
    <section class="pb-12 md:pb-16 bg-paper">
        <div class="container-prose">
            <div class="flex flex-col sm:flex-row gap-3">
                <a
                    href={externalLinks.register}
                    class="btn btn-sunstone text-base"
                    target="_blank"
                    rel="noopener noreferrer"
                >
                    {t.common.register}
                    <i class="fa-solid fa-arrow-right text-sm" aria-hidden="true"></i>
                </a>
                <a
                    href={externalLinks.demoBooking}
                    class="btn btn-secondary text-base"
                    target="_blank"
                    rel="noopener noreferrer"
                >
                    {t.common.demo}
                </a>
            </div>
        </div>
    </section>

    <section class="py-12 md:py-16 bg-paper">
        <div class="container-wide">
            <div class="grid grid-cols-1 md:grid-cols-4 gap-3 md:gap-4">
                <article class="md:col-span-2 md:row-span-2 bg-gradient-to-br from-sunstone-mist/70 via-sunstone-mist/25 to-paper rounded-2xl ring-1 ring-chalk-dark p-8 md:p-10 flex flex-col gap-7 transition duration-300 ease-out hover:ring-chalk-darker">
                    <div>
                        <p class="eyebrow mb-3">{p.heroEyebrow}</p>
                        <h2 class="text-3xl md:text-[2.25rem] font-semibold text-charcoal tracking-tight leading-[1.05]">
                            {p.heroHeading}
                            <span class="block">{p.heroHeadingLine}</span>
                        </h2>
                    </div>

                    <div class="bg-paper rounded-xl ring-1 ring-chalk-dark p-5 md:p-6 shadow-[0_12px_40px_-24px_rgba(25,25,25,0.18)] overflow-x-auto">
                        <div class="flex items-center justify-between gap-3 mb-4">
                            <p class="text-[11px] uppercase tracking-[0.12em] font-semibold text-gravel">
                                {p.mockupPermissionsPerRole}
                            </p>
                            <span class="text-[10px] uppercase tracking-[0.12em] font-semibold text-gravel">{p.mockupExample}</span>
                        </div>

                        <div class="grid grid-cols-[1fr_auto_auto_auto_auto] items-center gap-x-2 sm:gap-x-3 gap-y-2 text-sm min-w-[300px]">
                            <span class="text-[10px] uppercase tracking-[0.08em] font-semibold text-gravel"></span>
                            <span class="text-[9px] uppercase tracking-[0.06em] font-semibold text-gravel text-center leading-tight">{p.mockupColView}</span>
                            <span class="text-[9px] uppercase tracking-[0.06em] font-semibold text-gravel text-center leading-tight">{p.mockupColCreate}</span>
                            <span class="text-[9px] uppercase tracking-[0.06em] font-semibold text-gravel text-center leading-tight">{p.mockupColEdit}</span>
                            <span class="text-[9px] uppercase tracking-[0.06em] font-semibold text-gravel text-center leading-tight">{p.mockupColDelete}</span>

                            <div class="flex items-center gap-2 py-2 border-t border-chalk-dark">
                                <span class="inline-flex items-center justify-center h-6 w-6 rounded-md bg-sunstone-mist text-sunstone-deep ring-1 ring-sunstone-soft/40">
                                    <i class="fa-solid fa-user-shield text-[0.65rem]" aria-hidden="true"></i>
                                </span>
                                <span class="text-charcoal font-medium">{p.mockupRoleAdmin}</span>
                            </div>
                            <span class="flex items-center justify-center py-2 border-t border-chalk-dark text-sunstone-deep" aria-label={p.mockupAriaViewAllowed}>
                                <i class="fa-solid fa-check text-xs" aria-hidden="true"></i>
                            </span>
                            <span class="flex items-center justify-center py-2 border-t border-chalk-dark text-sunstone-deep" aria-label={p.mockupAriaCreateAllowed}>
                                <i class="fa-solid fa-check text-xs" aria-hidden="true"></i>
                            </span>
                            <span class="flex items-center justify-center py-2 border-t border-chalk-dark text-sunstone-deep" aria-label={p.mockupAriaEditAllowed}>
                                <i class="fa-solid fa-check text-xs" aria-hidden="true"></i>
                            </span>
                            <span class="flex items-center justify-center py-2 border-t border-chalk-dark text-sunstone-deep" aria-label={p.mockupAriaDeleteAllowed}>
                                <i class="fa-solid fa-check text-xs" aria-hidden="true"></i>
                            </span>

                            <div class="flex items-center gap-2 py-2 border-t border-chalk-dark">
                                <span class="inline-flex items-center justify-center h-6 w-6 rounded-md bg-chalk-light text-steel ring-1 ring-chalk-dark">
                                    <i class="fa-solid fa-boxes-packing text-[0.65rem]" aria-hidden="true"></i>
                                </span>
                                <span class="text-charcoal font-medium">{p.mockupRoleWarehouse}</span>
                            </div>
                            <span class="flex items-center justify-center py-2 border-t border-chalk-dark text-sunstone-deep" aria-label={p.mockupAriaViewAllowed}>
                                <i class="fa-solid fa-check text-xs" aria-hidden="true"></i>
                            </span>
                            <span class="flex items-center justify-center py-2 border-t border-chalk-dark text-sunstone-deep" aria-label={p.mockupAriaCreateAllowed}>
                                <i class="fa-solid fa-check text-xs" aria-hidden="true"></i>
                            </span>
                            <span class="flex items-center justify-center py-2 border-t border-chalk-dark text-sunstone-deep" aria-label={p.mockupAriaEditAllowed}>
                                <i class="fa-solid fa-check text-xs" aria-hidden="true"></i>
                            </span>
                            <span class="flex items-center justify-center py-2 border-t border-chalk-dark text-gravel/60" aria-label={p.mockupAriaDeleteNotAllowed}>
                                <i class="fa-solid fa-minus text-[0.65rem]" aria-hidden="true"></i>
                            </span>

                            <div class="flex items-center gap-2 py-2 border-t border-chalk-dark">
                                <span class="inline-flex items-center justify-center h-6 w-6 rounded-md bg-chalk-light text-steel ring-1 ring-chalk-dark">
                                    <i class="fa-solid fa-headset text-[0.65rem]" aria-hidden="true"></i>
                                </span>
                                <span class="text-charcoal font-medium">{p.mockupRoleCustomerService}</span>
                            </div>
                            <span class="flex items-center justify-center py-2 border-t border-chalk-dark text-sunstone-deep" aria-label={p.mockupAriaViewAllowed}>
                                <i class="fa-solid fa-check text-xs" aria-hidden="true"></i>
                            </span>
                            <span class="flex items-center justify-center py-2 border-t border-chalk-dark text-sunstone-deep" aria-label={p.mockupAriaCreateAllowed}>
                                <i class="fa-solid fa-check text-xs" aria-hidden="true"></i>
                            </span>
                            <span class="flex items-center justify-center py-2 border-t border-chalk-dark text-sunstone-deep" aria-label={p.mockupAriaEditAllowed}>
                                <i class="fa-solid fa-check text-xs" aria-hidden="true"></i>
                            </span>
                            <span class="flex items-center justify-center py-2 border-t border-chalk-dark text-gravel/60" aria-label={p.mockupAriaDeleteNotAllowed}>
                                <i class="fa-solid fa-minus text-[0.65rem]" aria-hidden="true"></i>
                            </span>

                            <div class="flex items-center gap-2 py-2 border-t border-chalk-dark">
                                <span class="inline-flex items-center justify-center h-6 w-6 rounded-md bg-sunstone-mist/60 text-sunstone-deep ring-1 ring-sunstone-soft/60">
                                    <i class="fa-solid fa-user-plus text-[0.65rem]" aria-hidden="true"></i>
                                </span>
                                <span class="text-charcoal font-medium">{p.mockupRoleIntern} <span class="text-[10px] uppercase tracking-[0.08em] font-semibold text-sunstone-deep ml-1">{p.mockupCustomRoleBadge}</span></span>
                            </div>
                            <span class="flex items-center justify-center py-2 border-t border-chalk-dark text-sunstone-deep" aria-label={p.mockupAriaViewAllowed}>
                                <i class="fa-solid fa-check text-xs" aria-hidden="true"></i>
                            </span>
                            <span class="flex items-center justify-center py-2 border-t border-chalk-dark text-gravel/60" aria-label={p.mockupAriaCreateNotAllowed}>
                                <i class="fa-solid fa-minus text-[0.65rem]" aria-hidden="true"></i>
                            </span>
                            <span class="flex items-center justify-center py-2 border-t border-chalk-dark text-gravel/60" aria-label={p.mockupAriaEditNotAllowed}>
                                <i class="fa-solid fa-minus text-[0.65rem]" aria-hidden="true"></i>
                            </span>
                            <span class="flex items-center justify-center py-2 border-t border-chalk-dark text-gravel/60" aria-label={p.mockupAriaDeleteNotAllowed}>
                                <i class="fa-solid fa-minus text-[0.65rem]" aria-hidden="true"></i>
                            </span>
                        </div>

                        <div class="flex items-center justify-between gap-3 pt-4 mt-4 border-t border-chalk-darker">
                            <span class="text-sm font-semibold text-charcoal">{p.mockupUsersLabel}</span>
                            <span class="inline-flex items-baseline gap-1.5">
                                <i class="fa-solid fa-infinity text-sunstone-deep text-base" aria-hidden="true"></i>
                                <span class="text-sm font-semibold text-sunstone-deep">{p.mockupNoCostPerUser}</span>
                            </span>
                        </div>
                    </div>

                    <p class="text-steel leading-relaxed text-sm md:text-[0.95rem]">
                        {p.heroBody}
                    </p>
                </article>

                {
                    subFeatures.map((feature) => (
                        <article class="bg-paper rounded-2xl ring-1 ring-chalk-dark p-6 md:p-7 flex flex-col gap-4 transition duration-300 ease-out hover:ring-chalk-darker">
                            <div class="flex items-center justify-center h-12 w-12 rounded-xl bg-sunstone-mist text-sunstone-deep ring-1 ring-sunstone-soft/40">
                                <i class={`${feature.icon} text-xl`} style="--fa-secondary-opacity: 0.32;" aria-hidden="true"></i>
                            </div>
                            <div>
                                <p class="text-[11px] uppercase tracking-[0.08em] font-semibold text-gravel mb-1.5">{feature.eyebrow}</p>
                                <h3 class="text-base md:text-lg font-semibold text-charcoal tracking-tight leading-snug">{feature.heading}</h3>
                                <p class="text-sm text-steel leading-relaxed mt-2">{feature.body}</p>
                            </div>
                        </article>
                    ))
                }
            </div>
        </div>
    </section>

    <section class="py-24 md:py-32 bg-chalk-light">
        <div class="container-prose">
            <div class="grid lg:grid-cols-12 gap-12 lg:gap-16 items-start">
                <div class="lg:col-span-5">
                    <p class="eyebrow mb-4">{p.permissionsEyebrow}</p>
                    <h2 class="text-3xl md:text-4xl font-semibold text-charcoal tracking-tight leading-[1.1]">
                        {p.permissionsHeading}
                        <span class="block text-sunstone-deep">{p.permissionsHeadingLine}</span>
                    </h2>
                    <p class="mt-6 text-base text-steel leading-relaxed">
                        {p.permissionsBody}
                    </p>
                </div>

                <div class="lg:col-span-7">
                    <ul class="divide-y divide-chalk-dark border-y border-chalk-dark">
                        {
                            permissionTypes.map((type) => (
                                <li class="py-7 flex items-start gap-5">
                                    <span class="flex-shrink-0 inline-flex items-center justify-center h-8 w-8 rounded-full bg-sunstone-mist text-sunstone-deep ring-1 ring-sunstone-soft/40 mt-0.5">
                                        <i class="fa-solid fa-key text-xs" aria-hidden="true"></i>
                                    </span>
                                    <div>
                                        <h3 class="text-lg font-semibold text-charcoal tracking-tight leading-snug">{type.title}</h3>
                                        <p class="mt-2 text-steel leading-relaxed text-[0.975rem]">{type.body}</p>
                                    </div>
                                </li>
                            ))
                        }
                    </ul>
                </div>
            </div>
        </div>
    </section>

    <section class="py-24 md:py-32 bg-paper">
        <div class="container-prose">
            <div class="grid lg:grid-cols-12 gap-12 lg:gap-16">
                <div class="lg:col-span-4">
                    <p class="eyebrow mb-4">{p.faqEyebrow}</p>
                    <h2 class="text-3xl md:text-4xl font-semibold text-charcoal tracking-tight leading-[1.1]">
                        {p.faqHeading}
                        <span class="block text-sunstone-deep">{p.faqHeadingLine}</span>
                    </h2>
                    <p class="mt-6 text-base text-steel leading-relaxed">
                        {p.faqBody}
                    </p>
                    <a href={localizeHref('/contact', locale)} class="group mt-8 inline-flex items-center gap-2 text-charcoal font-semibold text-sm">
                        {p.faqCta}
                        <i class="fa-solid fa-arrow-right text-xs transition-transform duration-200 group-hover:translate-x-1" aria-hidden="true"></i>
                    </a>
                </div>

                <div class="lg:col-span-8">
                    <FaqList items={faqs} />
                </div>
            </div>
        </div>
    </section>

    <CtaBanner />
</PageLayout>
